# Project manifest

What this project is, written for an agent that has never seen it. Ten honest lines beat three pages of aspiration.

## What this project does

One paragraph. What it is for, and who uses it.

## Stack

| Layer | Choice | Notes |
| --- | --- | --- |
| Language | | |
| Framework | | |
| Data store | | |
| Build / package manager | | |
| Deploy target | | |

## How to run the tests

The exact command, from the repository root. If there is more than one, say which one gates a merge.

```bash

```

## What must never break

The invariants worth failing a build over. Be specific enough that a reviewer could check one.

-
-

## Conventions, and where they are written down

Point at the files rather than restating them. An agent that can find your linter config does not need you to paraphrase it.

| Convention | Where it lives |
| --- | --- |
| Code style | |
| Commit messages | |
| Branch naming | |
| Architecture decisions | |

If a row here is empty, that gap is a capability-map row: a reviewer with nothing to cite produces opinions rather than verdicts.

## What a change normally touches

The paths a typical change lands in, and the paths that need a human's attention when they change.

| Area | Path | Needs a human |
| --- | --- | --- |
| | | |
