# W-1 · Preflight Setup

Workshop. Part of the [two-day module set](./README.md).

> **TODO, content PR.** Placeholder. No page in this repo covers this block yet.

Wraps [`bootstrap/deps.sh`](../bootstrap/deps.sh) into one pass-or-fail line, with a local path and a cloud path. The cloud path checks that a box credential is configured and connects, so it ships stubbed behind a not-yet message until the participant box CLI lands.
